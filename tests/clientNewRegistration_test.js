const {
    basePage,
    accounts,
    clientLogin
} = inject();

Feature('prod client Registration').retry();

Scenario('PROD Тест регистрации нового клиента', async ({I}) => {

}).tag('prodClientLogin1');